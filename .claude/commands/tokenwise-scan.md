Run tokenwise scan on the latest AI coding agent session. Execute:

```bash
npx @davizin713/tokenwise scan --agent auto --model sonnet
```

Show the cost breakdown, top token hogs, and cache performance to the user. If the session cost is high, suggest running `tokenwise audit` to trim instruction files.
